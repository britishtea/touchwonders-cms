<!DOCTYPE html>
<html>
    <head>
        <title></title>
        <style>
            * { padding: 0; margin: 0; }

            body {
                color: #333;
                font-family: Helvetica;
            }
            #wrapper { margin: 0 auto; padding: 1em; width: 75%; }
            header { margin-bottom: 2em; vertical-align: bottom; height: 5em; }
            header .title { font-weight: bold; }

            h1 { margin: 2em 0 1em 0; }

            form { margin-bottom: 2em; width: 50%; }
            form img { float: right; height: 50%; width: 50%; }
            label { display: block; margin-top: 1em; }
            input { font-size: 1em; margin: 0.5em 0; max-width: 100%; min-width: 90%; }

            #full_image img { max-width: 100%; }

            .image_preview { margin: 1em 0; padding: 0.5em; }
            .image_preview img { display: inline-block; height: 50px; margin-right: 0.5em; width: 50px; }
            .image_preview .meta { display: inline-block; height: 50px; vertical-align: top; }
            .image_preview li { color: gray; display: inline-block; min-width: 4em; }

            li { list-style-type: none; }

            .search { margin: 0 auto; text-align: center; }
        </style>
    </head>

    <body>
        <div id="wrapper">
            <header>
                <% if logged_in? %>
                    <span class="title">Menu</span>

                    <ul id="navigation">
                        <li><a href="/images">Home</a></li>
                        <li><a href="/image">Upload image</a></li>
                        <li><a href="/logout">Logout</a></li>
                    </ul>
                <% end %>
            </header>

            <%= yield %>
        </div>
    </body>
</html>
